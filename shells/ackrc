# Ignore case in the search strings if PATTERN contains no uppercase characters.
# This is similar to "smartcase" in vim. This option is off by default, and
# ignored if "-i" is specified.
--smart-case

# Ignore Virtualenv directories
--ignore-dir=venv

# Ignore Jekyll generated directories
--ignore-dir=_site

# Ignore (.)Bundle directories
--ignore-dir=bundle
--ignore-dir=.bundle

# Ignore SQLite db files
--ignore-file=ext:db

# Ignore Build output directories
--ignore-dir=build
